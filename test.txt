            content+=`<div class="col-sm-6">`;
                content+=`<input id="ten_ship" type="text" placeholder="Tên người nhận"`;
                    content+=`value="{{ Auth::guard('customer')->user()->ho_va_ten }}">`;
            content+=`</div>`;
            content+=`<div class="col-sm-6 mb-2">`;
                content+=`<input id="phone_ship" type="text" placeholder="Số điện thoại người nhận"`;
                    value="{{ Auth::guard('customer')->user()->so_dien_thoai }}">
            content+=`</div>`;
            content+=`<div class="col-12">`;
                content+=`<textarea id="dia_chi_ship" cols="30" rows="2" placeholder="Địa chỉ người nhận">{{ Auth::guard('customer')->user()->dia_chi }}</textarea>`;
            content+=`</div>`;
